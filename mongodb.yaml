- name: Configuring mongodb
  hosts: mongodb
  become: yes
  roles: #this will automatically load a folder called roles in the current working directory and look for a folder with the same name as the component
    - mongodb
